import type { Config } from 'tailwindcss';

const config: Config = {
    // This is just for tooling compatibility, actual config is in globals.css
    content: ['./src/**/*.{js,ts,jsx,tsx,mdx}']
};

export default config;
